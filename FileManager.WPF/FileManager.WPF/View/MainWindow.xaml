<Window x:Class="FileManager.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileManager.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="#FFC8C8C8">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="True">
            <Button Margin="5,2,2,2" Height="26" FontSize="15" HorizontalAlignment="Left">Back</Button>
            <TextBox Name="PathString" Text="{Binding Path=FullPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Margin="5,2,5,2" Background="#FFEFEAEA"/>
        </DockPanel>
        <ListBox Name="dirList" Margin="5" Grid.Row="1" ItemsSource="{Binding Directoryes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedFile, Mode=TwoWay, UpdateSourceTrigger=Explicit}">
            <ListBox.InputBindings>
                <!--<MouseBinding Gesture="LeftDoubleClick"/>-->
                <KeyBinding Gesture="Enter" Command="{Binding ListBoxItemDoubleClick}"/>
            </ListBox.InputBindings>
        </ListBox>

    </Grid>
</Window>
