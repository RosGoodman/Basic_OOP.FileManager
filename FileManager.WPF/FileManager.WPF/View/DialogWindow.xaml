<Window x:Class="FileManager.WPF.View.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileManager.WPF.View"
        mc:Ignorable="d"
        Title="Новое имя файла." Height="120" Width="250">
    <Grid Background="#FF929292">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>
        <TextBox Height="25" Width="200" Background="Gainsboro" VerticalAlignment="Center" HorizontalAlignment="Center"
                   Text="{Binding NewFileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <WrapPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="7">
            <Button IsDefault="True" Click="Accept_Click" MinWidth="60" Margin="0,0,10,0">OK</Button>
            <Button IsCancel="True" MinWidth="60" Click="Button_Click">Отмена</Button>
        </WrapPanel>
    </Grid>
</Window>
